/*Основной заголовочный файл, подключаемый к android-приложению. Доступна только одна функция AnalyseMusic, в которой происходит весь анализ файла.
*/
#include <stdio.h>

bool AnalyseMusic(char*, char*);